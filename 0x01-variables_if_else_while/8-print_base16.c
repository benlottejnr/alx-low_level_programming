#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always 0 (Success)
*/
int main(void)
{
	int n;
	
	for (n = 0; n <= 9; n++)
	putchar(n);
	for (char n = 'a'; n <= 'f'; n++)
	putchar('\n');
	return (0);
}
