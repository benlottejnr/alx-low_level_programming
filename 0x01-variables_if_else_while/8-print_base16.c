#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always 0 (Success)
*/
int main(void)
{
	int n;
	char ch;
	for (n = 0; n <= 16; n++)
	putchar(ch);
	for (char n = 'a'; n <= 'f'; n++)
	putchar('\n');
	return (0);
}
