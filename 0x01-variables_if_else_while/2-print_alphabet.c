#include <stdio.h>

/**
 * main - Entry point - checks if a number is positive or negative
 *
 * Return: always 0 (Success)
*/
int main(void)
{
	int ch;
	
	for (ch= ‘a’; ch<= ‘z’; ch++)
	putchar(ch);
	putchar('\n');
	return (0);
}
