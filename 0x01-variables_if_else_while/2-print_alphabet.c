#include <stdio.h>
#include <ctype.h>

/**
 * main - Entry point - checks if a number is positive or negative
 *
 * Return: always 0 (Success)
*/
int main(void)
{
	int ch;
	
	ch = tolower(ch)
	putchar(ch);
	putchar('\n')
	return (0);
}
