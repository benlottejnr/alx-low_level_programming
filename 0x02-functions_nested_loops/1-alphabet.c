#include "main.h"

/**
 * main - Entry point
 *
 * Return: always 0 (Success)
*/
void print_alphabet(void)
{
	int alpha;
	for (int alpha = 'a'; alpha <= 'z'; alpha++)
	_putchar(alpha)
	_putchar('\n');
}
